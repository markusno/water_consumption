export const selectConsumptionHistory = state => {
  return state.consumptionHistory
}

export const isFetching = state => {
  return state.isFetching
}

export const view = state => {
  return state.view
}